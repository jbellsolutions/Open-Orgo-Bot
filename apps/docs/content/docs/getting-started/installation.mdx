---
title: Installation
description: Install Open Orgo Bot on macOS, Windows, or Ubuntu.
icon: Download
---

## Download a released build

Choose your computer below. Each link always downloads the latest published build.

| Platform | Recommended package | Notes |
|---|---|---|
| macOS Apple silicon | [Download `.dmg`](https://github.com/jbellsolutions/open-orgo-bot/releases/latest/download/Open-Orgo-Bot-arm64.dmg) | Signed and notarized. Drag to Applications. |
| macOS Intel | [Download `.dmg`](https://github.com/jbellsolutions/open-orgo-bot/releases/latest/download/Open-Orgo-Bot-x64.dmg) | Signed and notarized. |
| Windows x64 | [Download installer](https://github.com/jbellsolutions/open-orgo-bot/releases/latest/download/Open-Orgo-Bot-setup.exe) | Per-user installer; Windows signing is not yet available. |
| Ubuntu 24.04 x64 | [Download `.deb`](https://github.com/jbellsolutions/open-orgo-bot/releases/latest/download/Open-Orgo-Bot-amd64.deb) · [AppImage](https://github.com/jbellsolutions/open-orgo-bot/releases/latest/download/Open-Orgo-Bot.AppImage) | The `.deb` is recommended; the AppImage is portable. |

<Callout type="warn" title="Windows SmartScreen">
  The Windows installer is not code-signed yet. Windows may show “Unknown publisher.” Verify that you downloaded it from the official Open Orgo Bot releases repository before choosing **More info → Run anyway**.
</Callout>

## Install an agent CLI

Open Orgo Bot needs at least one supported agent engine installed and signed in. Claude and Codex are the most common starting points.

1. Install the CLI from its official provider.
2. Run it once in a terminal and complete sign-in.
3. Restart Open Orgo Bot.
4. Open the model picker. Available engines appear normally; unavailable engines explain what is missing.

See [Agent engines](../providers/index.mdx) for the complete list and explicit path overrides.

## Using OpenMausBot at work

If your company uses OpenMaus Admin, you can sign in with your organization to use the AI models it provides, alongside your own. It is optional. See [Use OpenMausBot at work](./organisation.mdx).

## Build from source

Requirements: Node.js 24 or newer, pnpm 10.33.0, and at least one authenticated agent CLI.

```bash
git clone https://github.com/jbellsolutions/open-orgo-bot.git
cd open-orgo-bot
corepack enable
pnpm install --frozen-lockfile
```

Run the harness, web UI, and Electron shell in separate terminals:

```bash
pnpm dev:server
pnpm dev
pnpm dev:desktop
```

## Where data lives

Open Orgo Bot stores its application data under `~/.openmausbot`. Electron window and browser data use the normal per-platform application data directory.
